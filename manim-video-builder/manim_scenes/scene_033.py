from __future__ import annotations

from manim import *
from visual_beats import VisualBeatScene


class GeneratedVideo(VisualBeatScene):
    def construct(self):
        self.show_scene(
            title='Huấn luyện an toàn qua World Models',
            voice='Khám phá an toàn bên trong mô phỏng không gian ẩn. Sự an toàn của trí tuệ nhân tạo là vấn đề nhức nhối hiện nay. A I không thể tự ý thử nghiệm việc tăng liều thuốc để xem bệnh nhân có phản ứng xấu không. Khi tích hợp mô hình thế giới nhân quả, A I được cung cấp một không gian giả lập an toàn để tự vấn phản thực tế. Nó có thể hỏi: Nếu tôi đưa ra quyết định tồi tệ này, hậu quả kinh khủng nhất là gì? Mô hình sẽ mô phỏng kết quả thảm khốc để cảnh báo chính sách hành động. Đây là lớp phòng thủ cuối cùng, ngăn chặn trí tuệ nhân tạo thực thi các chính sách gây hại chưa từng có tiền lệ.',
            formula='Safe Exploration \\in Latent Simulation',
            visual_hints=['Bác sĩ robot do dự trước ống tiêm thuốc đỏ nguy hiểm.', 'Robot tự mô phỏng hành động tiêm thuốc bên trong đám mây suy nghĩ, bệnh nhân ảo xuất hiện phản ứng phụ.', 'Màn hình cảnh báo màu đỏ chớp tắt, robot lập tức thay đổi quyết định, cất ống tiêm đỏ đi.'],
            pattern='World Model Rollout',
            duration=36.264,
        )
