from __future__ import annotations

from manim import *
from visual_beats import VisualBeatScene


class GeneratedVideo(VisualBeatScene):
    def construct(self):
        self.show_scene(
            title='Đánh giá AI qua Physical IQ',
            voice='Điểm chuẩn đánh giá suy luận vật lý. Làm thế nào để đo đạc chỉ số I Q vật lý của một trí tuệ nhân tạo? Các hệ thống Benchmark giả lập môi trường 3D tương tác ra đời. Mô hình A I được cho xem một tình huống, ví dụ như xếp một tòa tháp nghiêng. Nó phải dự đoán tháp sẽ đổ về hướng nào hoặc có đổ hay không. Nếu không có một World Model chất lượng kết hợp biểu diễn đối tượng, A I sẽ chỉ đoán bừa và nhận số điểm bằng 0 trong các bài kiểm tra này. Các chỉ số này là thước đo chuẩn xác nhất cho chặng đường tiến tới A G I thực thụ có khả năng thao tác vật lý.',
            formula='Physical Reasoning Benchmark',
            visual_hints=['Màn hình đồ họa hiển thị các bài test như cân thăng bằng, đổ nước, xếp khối gỗ.', 'Tòa tháp các khối gỗ đang chông chênh, A I hiển thị một véc tơ màu đỏ dự báo điểm sụp đổ.', 'Biểu đồ cột điểm số hiển thị sự vượt trội của mô hình có lý thuyết nhân quả so với A I cổ điển.'],
            pattern='Vector to Slots',
            duration=35.112,
        )
